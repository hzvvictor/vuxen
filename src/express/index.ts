import Controller from "./Controller";
import useErrorHandler from "./useErrorHandler";

export { Controller, useErrorHandler };

const express = {
  Controller,
  useErrorHandler
};

export default express;