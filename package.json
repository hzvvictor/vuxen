{
  "name": "vuxen",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "publish": "npm run build && npm version patch && cd dist && npm version patch && npm publish  && cd .. && git add . && git commit -m 'chore: release new version' && git push"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.4",
    "express": "^4.21.0",
    "typescript": "^5.6.2"
  }
}
